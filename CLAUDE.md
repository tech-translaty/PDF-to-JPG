## Sync policy
At the start of every session, run git fetch and report whether this branch is behind origin. If behind and the working tree is clean, git pull before doing anything else. At the end of every work session, offer to commit and push so all devices stay in sync.
